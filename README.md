# About
A command-line tool for quickly extracting Kindle highlights into either JSON or Markdown.

# Usage
use `python extractor.py -h` for a list of all arguments.
common usage is `python extractor.py -i "My Clippings.txt" -m markdown -o "Markdown/"`